# Проект Genius

## Курс обучения WordPress

Автор курса: Александр Сокирка.

В этом файле я постараюсь описать то, что я сделал обучаясь в данном курсе. Так как первые 11 разделов уроков в первом модуле были посвящены ознакомлению с _PHP_ - второй раздел начинает нумероваться со следующего числа _"12"_. Вот с него и начнем второй модуль _"Codex для разработки темы на WordPress"_.

## 2. Codex для разработки темы на WordPress

### 12. Установка WordPress

Скачал архив WordPress 7.0 с <a href="https://wordpress.org/download/">сайта разработчика</a>. Распаковал в директорию моих доменов `/domains/genius.loc`.

Развернул домен на сервере OSPanel 6.51, его можно скачать с <a href="https://ospanel.io/download/">сайта разработчика</a>. Для этого пришлось создать файл настройки в _.osp/project.ini_. Каталог _.osp_ добавил в игнор созданным в корне сайта файле _.gigignore_.

В phpMyAdmin создал БД в MySQL: `genius`. Имя пользователя _root_, пароль пустой, хост _MySQL-8.4_. При развертывании на другом сервере необходимо будет поменять хост на _localhost_.

Настроил базу данных при установке WordPress.

После всего этого удалил файлы из корневого каталога сайта: _license.txt_, _reacme.html_, _wp-config-sample.php_, чтобы не было соблазна сломать извне.

Пока буду коммитить весь WordPress, но возможно, что мне нужно будет работать только с каталогом _genius_, находящимся в _wp-content/themes/_, который я создам позднее.

### 13. Стартовая тема для WordPress

Темы в WordPress называют _Шаблонами_, но правильно их все-таки назвать _Темами_ от английского _themes_.

Стартовую тему можно найти на:

- <a href="https://github.com/tidythemes/blankslate">GitHub</a>. Здесь находится пример пустой стартовой темы;
- <a href="https://underscores.me/">Underscores</a> - генератор тем для WordPress.

На _Underscores_ я и сгенерировал стартовую тему, которая скачалась zip-архивом и далее я ее распаковал в _wp-content/themes/geniuscourse_. В Админской панели, перейдя в темы эта тема появилась. Активировал тему. Но необходимо смотреть, чтобы у новой стартовой темы не было уведомлений на обновление - это будет означать, что тема с таким именем в бесплатных шаблонах WordPress уже существует. Лучше будет если такая тема не будеть сущевтвовать не только в бесплатных шаблонах WordPress, но и на территории всего Интернета.

Первым делом открыл файл _style.css_. WordPress первым делом смотрит на этот файл. Если этого файла не будет, WordPress выдаст предупреждение, что тема отсутствует или не работает. В этом файле есть метатеги, находящиеся в начальном комментарии. Я их подкорректировал:

```
/*!
Theme Name: geniuscourse
Theme URI: #
Author: Angol ang0l@inbox.ru
Author URI: https: //github.com/ang0l
Description: Моя первая тема на WordPress
Version: 1.0.0
Tested up to: 5.4
Requires PHP: 5.6
License: GNU General Public License v2 or later
License URI: LICENSE
Text Domain: geniuscourse
Tags: custom-background, custom-logo, custom-menu, featured-images, threaded-comments, translation-ready
*/
```

- **Theme Name: geniuscourse** - Название темы.
- **Theme URI: #** - установлена заглушка. Если это коммерческий продукт - нужна ссылка, если это для клиента или для эсперемента можно поставить заглушку или вовсе удалить эту строку.
- **Author: Angol** - Ник или имя автора.
- **Author URI: https: //github.com/ang0l** - ссылка на портфолио, если компания, то ссылка на компанию.
- **Description: Моя первая тема на WordPress** - описание темы.
- **Version: 1.0.0** - версия темы. Каждый раз после ее редактирования ей нужно увеличивать версию.
- **Tested up to: 5.4** - на какой версии WordPress тестировалась тема.
- **Requires PHP: 5.6** - на какой версии _PHP_ работает и тестировалась тема.
- **License: GNU General Public License v2 or later** - лицензия. Остается так.
- **License URI: LICENSE** - URL лицензии. Остается так.
- **Text Domain: geniuscourse** - Text Domain. Необходим для распознавания в коде. Складывается из названия темы. Если в названии темы есть заглавные буквы, они переводятся в строчные, если есть пробел, то вместо него ставится дефис.
- **Tags: custom-background, custom-logo, custom-menu, featured-images, threaded-comments, translation-ready** - теги по которым будут находить тему. Строку можно удалить если она не является коммерческим продуктом или продуктом для бесплатных шаблонов WordPress.

Добавил дамп БД сайта.

### 14. Иерархия файлов WordPress темы

Из новоиспеченной темы удалил файлы не влияющие на работоспостобность темы:

- .eslintrc
- .stylelintrc.json
- composer.jsom
- package.json
- phpcs.xml.dict
- README.md

Оставшиеся файлы:

- 404.php - шаблон для несуществующих страниц;
- archive.php - шаблон для архивных записей (на сколько я понял: посты имеющие дату поста, т.е. находящиеся в некоем архиве);
- comments.php - шаблон для формы и листинга всех комментариев;
- footer.php - шаблон для вставки подвала моей темы;
- functions.php - очень важный файл, он является окном в коды WordPress, сюда можно добавлять необходимые функции, редактировать текущие...;
- header.php - шаблон для шапки моей темы;
- index.php - очень важный файл, является листингом для блог-постов, WordPress первым делом смотрит в него;
- LICENSE - файл лицензии, который должен быть в корне темы;
- page.php - шаблон для открытой страницы;
- readme.txt - файл содержащий необходимую информацию по теме, если эта тема для клиента;
- screenshot.png - файл скриншота, этот файл присутствует в WordPress, т.к. этот файл необходим для CMS, но сейчас это пустой файл;
- search.php - шаблон для архива результата поиска;
- sidebar.php - колонка сайта, в ней содержатся виджеты;
- single.php - шаблон для сингл поста (открытого поста);
- style-rtl.css - стили для арабского языка (справа на лево);
- style.css - стили для темы.

На данный момент _Рубрики_ (_category_) используют файл _archive.php_. Но если в корне темы создать пустой файл _category.php_, то WordPress покажет этот файл - пустую страницу. В приоритете для _Рубрик_, если присутствует файдл _category.php_, то будет использоваться он, если такой файл отсутствует - будет использоваться _archive.php_.

Тоже самое происходит с _Автором_.

Удалил файл _style-rtl.css_ за ненадобностью, т.к. я не буду работать с арабским языком.

Не забыть сделать дамп БД.

Документация WordPress:

1. Документация по иерархии файлов - https://developer.wordpress.org/themes/basics/template-hierarchy/

### 15. Подключение файлов кода PHP

За подключение файлов кода _PHP_ отвечает функция `get_template_part()`. Пропишу эту функцию в файле _header.php_ в самом начале `<body>`. Далее в корень темы добавлю директорию _partials_ (части кода), в ней создам файлы _part.php_, _part-one.php_, _part-two.php_. Файлы с дефисом называются спецификацией. В этих файлах пропишу следующий код:

```
<?php

echo 'X';

```

, где X - номера файлов 0, 1, 2.

В функции `get_template_part()` задаю параметры где нужно искать эти коды:

- `get_template_part('parts/part')` - открывает код из файла _partials/part.php_, сверху страницы выводится _0_;
- `get_template_part('parts/part', 'one')` - открывает код из файла _partials/part-one.php_, вторым параметром передается спецификация, сверху страницы выводится _1_;
- `get_template_part('parts/part', 'two')` - открывает код из файла _partials/part-two.php_, вторым параметром передается спецификация, сверху страницы выводится _2_.

Третим параметром передается архив с дополнительными параметрами. Мне сейчас дополнительные параметры не нужны, и я его не передаю.

В этом курсе я не буду использовать директорию _template-parts_, в ней лежат файлы контента с похожими названиями описаными чуть выше. Я ее удалю вместе с файлами.

**Итог**: я научился в WordPress выводить части кода из ранее подготовленных файлов.

Документация WordPress:

1. Функция get_template_part() - https://developer.wordpress.org/reference/functions/get_template_part/

### 16. Подключение CSS стилей и JS скриптов

В этом уроке буду учтиься подключать стили и скрипты специальными хуками и функциями. Так же познакомлюсь с понятием "хука".

- В файле _header.php_ удаляю код с предыдущего урока, а так же весь ниже написанный код. Он мне не нужен.
- В файле _footer.php_ удаляю код с контейнером `<div>`. Оставляю только функцию `wp_footer()`. В файле _header.php_ так же присутствует похожий файл - `wp_header()`.
- В файле _functions.php_ буду дорабатывать. Вначале буду писать свой код и по мере надобности буду переносить или удалять код написанный ниже.

Коды написаны в _function.php_ с ними присутствуют комментарии. и хук для этой функции

Удаления:

- Удалил из корня темы каталог _js_.
- Удалил в файле _functions.php_ функцию `geniuscourse_scripts()` и хук для этой функции.
- Удалил код из файла _assets/js/script.js_, чтобы он не менаш мне работать.
- Удалил стили из файла _style.css_, я уже ими не пользуюсь, поэтому они мне не нужны.

В коре WordPress в файле _wp-config.php_ изменил `define( 'WP_DEBUG', false )` значение на `true` для того, чтобы выводились все ошибки для отладки.

Документация WordPress:

1. Подключение CSS стилей - https://developer.wordpress.org/reference/functions/wp_enqueue_style/
2. Регистрация CSS стилей - https://developer.wordpress.org/reference/functions/wp_register_style/
3. Подключение JS - https://developer.wordpress.org/reference/functions/wp_enqueue_script/
4. Регистрация JS скриптов - https://developer.wordpress.org/reference/functions/wp_register_script/

### 17. Хуки для Шапки и Подвала

Хук для шапки - `wp_head()`, хук для подвала - `wp_footer()`. Для того, чтобы код выводился в шапке или в подвале необходимо функцию вывести через эти хуки.

```
function geinuscourse_meta_tag()
{
	echo '<meta name="author" content="Angol">';
}
add_action('wp_head', 'geinuscourse_meta_tag'); // Мета-тег выводится в <head>
```

```
function geinuscourse_meta_tag()
{
	echo 'Привет Гениальный курс!<br>';
}
add_action('wp_body_open', 'geinuscourse_meta_tag'); // текст выводится в теле вначале <body>
```

В шапке (_header.php_):

- `wp_head()` обязательно должен присутсвовать внутри тегов `<head></head>`. `wp_body_open` обязательно должен присутствовать вначале тела, т.е. после тега `<body>`.
- `body_class()` обязательно должен присутствовать как параметр тега `<body>`: `<body <?php body_class() ?>>`. Здесь будут содержаться классы необходимые тегу `<body>`. Классы необходимо задавать как параметр хука, т.е. если нужен дополнительный класс, то он задается в параметре, а к нему уже будут выводится те классы, которые заложены в функции.
- Также необходимо, для автоматической генерации языка сайта присутствие `language_attributes()` в теге `<html>` в виде параметра: `<html <?php language_attributes() ?>>`

В подвале (_footer.php_):

- `wp_footer()` обязательно присутствует.

```
function geinuscourse_meta_tag()
{
	echo 'Мои контакты<br>';
}
add_action('wp_footer', 'geinuscourse_meta_tag'); // текст выводится в конце контента перез закрывающимся тегом </body>
```

Хуки принимают третий параметр - это приоритет. Например подгрузка скриптов имеет приоритет `20` и если я поставлю в подвале приоритет `10` то внешне ничего не изменится, но в коде скрипры будут выводиться после текста. При приоритете более `20` текст выведится после скриптов.

### 18. Реализация навигации. Админка и Фронт

Для управления меню нужно зайти в "Внешний вид" ("Appearance") -> "Меню" ("Menu").

При нажатии кнопки "Создать меню" создается навигация. Можно создавать столько навигационных пунктов меню, сколько мне угодно. Далее сохраняю меню, также добавляю новое и теперь у меня уже 2 навигационных меню. Но пока они не выводятся на сайте. Они будут выводиться только после того, как я задам им определенный "Display location" ("Расположение меню").

На данный момент существует одна локация "Pimary". Это локация болванки Underscores. Создавать новую локацию я буду в коде.

Удалил код в _functions.php_ из предыдущего урока, он мне не нужен.

Добавил функцию `function geniuscourse_registration_menus()` (см. код в файле _functions.php_). После этого у меня появились еще две локации: "Header navigation" и "Footer navigation". Также в функции `geniuscourse_setup()` закомментировал вывод функции `register_nav_menus()` чтобы не выводилась локация "Primary".

В Админке свой "Header" привязал к "Header navigation".
Для этого выбрал навигацию "Header" нажал "Выбрать" ("Select"), добавил в навигации пукт меню страницу "Sample page" ("Примеры страниц"), и произвольную ссылку ("Custom links") "https://geniuscourse.com" с анкором "Link". И все это сохранил.

Но и сейчас эта навигация нигде не выводится.

В файле _header.php_ в необходимом месте, у меня это - сразу после отрытия _body open_, вставляю код:

```
	<?php
	wp_nav_menu([
		'theme_location' => 'header_nav',
	])
	?>
```

Функция `wp_nav_menu()` выводит навигацию, в массиве задаются параметры: `them_location` со значением, которое задали в _functions.php_ -> `function geniuscourse_registration_menus()` -> `register_nav_menus()`: _header_nav_.

Тоже самое сделал с навигацией "Меню 1" переименовав ее в "Footer", привязав ее к "Footer navigation" и добавил немного другие пункты меню, вернее они были по умолчанию: "Главная" и "Примеры страниц". В файл _footer.php_, перед началом подвала добавил почти такойже код как и в шапке:

```
<?php
wp_nav_menu([
    'theme_location' => 'footer_nav',
])
?>
```

У функции `wp_nav_menu()` есть интересный параметр "menu". Его можно задать в подвале, например так `['menu' => 'Header']`, тогда вместо подвальной навигации будет выводится навигация шапки (она у нас подготовлена в _functions.php_) игнорируя `['theme_location' => 'footer_nav']`. Но этого делать не рекомендуется по причине: если тема пишется для коммерческих целей, то не известны заранее какие будут навигации присутствовать.

Документация WordPress по регистрации навигации:

1. Регистрация навигации - https://developer.wordpress.org/reference/functions/register_nav_menus/
2. Вывод навигации во фронте - https://developer.wordpress.org/reference/functions/wp_nav_menu/

### 19. Реализация формы поиска

В _functions.php_ пенеименована функция `geniuscourse_registration_menus()` в `geniuscourse_theme_init()`. В нее добавлена реализация функции `add_theme_support()` для поддержки HTML5 в формах, комментариях и пр.

Создан файл _searchform.php_ в каталоге темы с кодом:

```
<form method="get" action="<?= home_url('/') ?>">
    <input type="search" name="s" value="<?php the_search_query() ?>">
    <input type="submit">
</form>
```

- в _action_ функция `home_url('/')` выводит на главную страницу;
- в теге `<input>`:
  - параметр `name="s"`, именно к `s` прикрепляется get-запрос;
  - параметр в _value_ задается значение функции `the_search_query()`, эта функция выводит последний запрос и он сразу же помещается в поле ввода.

Форма ищет запрос во всех разделах сайта. Чтобы поиск происходил в определенных разделах, например посты, статьи или комментарии, необходимо дописать скрытое поле:

```
<input type="hidden" value="post" name="post_type">
```

По этому коду поиск будет производиться только в постах.

Кастомизировать форму поиска можно и из файла _functions.php_. Можно за ненадобностью удалить файл _searchform.php_ и в _functions.php_ добавить следующую функцию:

```
function geniuscourse_custom_search()
{

	$form = '<form method="get" action="<?= home_url("/") ?>">
    <input type="search" name="s" value="<?php the_search_query() ?>">
    <input type="submit">
</form>';

	return $form;
}
add_filter('get_search_form', 'geniuscourse_custom_search');

```

здесь важно то, чтобы у поля ввода поиска _name_ имел значение _s_. Но с этим моментом у меня будет хуже. Дело в том, что уроки старые (2022), а я их учу в 2026, версия WordPress по урокам 5.Х, а у меня 7.0. Возможно, что и у Underscores произошли какие-то изменения, т.к. еще и PHP более новый. В общем у меня не исчезают стили формы поиска по умолчанию болванки, как удалить эту фомру я пока не знаю, как изменить ее стили я пока не знаю. И при вставке формы в файл _functions.php_ эта форма должна быть заполнена (выводится сообщение "Вы пропустили это поле"). Думаю, что позднее разберусь с этим.

Документация WordPress по отображению формы поиска:

1. Отобразить форму поиска - https://developer.wordpress.org/reference/functions/get_search_form/

### 20. Безопасный вывод данных (Escaping)

В этом уроке я познакомлюсь с таким понятием как escaping. В WodrPress, в общем как и вообще в Web'е, всю информацию, которая выводится на экран, необходимо очищать от различного кода. Это же касается и информации, которая отправляется в БД.

Работаю в файле _header.php_.

`esc_url()` необходимо применять во всех ссылках, даже тех, которые выводятся функциями, например `home_url()`.
`wp_kses()` имеет 2 парамтеро: строка с тегами и массив атрибутов для этого тега. задает то, что нужно интерпретировать. Например:

```
$name = '<a href="#">Andrey Golovushkin <strong>Angol</strong></a>';

// интерпритирую sthong:

echo wp_kses($name, [
	'strong' => [] // пустой массив потому что нет атрибутов
]);
// Andrey Golovushkin **Angol**

// интерпритирую ссылку:
echo wp_kses($name, [
	'a' => [
		'href' => [], // атрибут href
		'title' => [], // атрибут title, если нужен
	]
]);
// Andrey Golovushkin Angol // текст ввиде ссылки

// интерпритирую ссылку и стронг (все другие теги урезаю):
echo wp_kses($name, [
	'a' => [
		'href' => [], // атрибут href
		'title' => [], // атрибут title, если нужен
	],
	'sthong' => [],
]);
// Andrey Golovushkin **Angol** // текст ввиде ссылки
```

Самые популярные ескейпы:

- `esc_attr()` - используется для ескейпа атрибутов
- `esc_html()` - для ескейпа html-тегов
- `esc_url()` - для ссылок
- `wp_kses()` - имеет 2 парамтеро: тег и массив атрибутов для этого тега. пропускает только тот html, который я разрешаю
- `wp_kses_post()` - содержит все разрешенные для поста теги. похож на wp_kses(), но с 1 параметром
- `wp_kses_data()` - содержит все разрешенные для комментариев теги. похож на wp_kses(), но с 1 параметром
- `esc_js()` - для вставки js-кода, например в кнопку обработчик событий
- `esc_textarea()` - предназначен для текстового поля textarea

Документация WordPress по ескейпу:

1. Escaping WordPress - https://developer.wordpress.org/plugins/security/securing-output/

### 21. Подготовка файлов темы к переводу

Интернационализация касается не контента сайа, контент пишен пользователь, а касается она больше интерфейса, файлов, там, где есть какие-нибудь названия, имена, кнопок, строк и прочего.

Для этого есть функции:

```
	$hello = __('Hello', 'geniuscourse'); // возвращает результат перевода
	$hello = _e('Hello', 'geniuscourse'); // выводит на экран результат перевода
```

Для подготовки темы к интернационализации мало просто переводить, необходимо еще ескейпить выеденный результат. Но ескейпить можно не помещая реультат в функции ескейпа, а для этого есть свои функции:

```
	$hello = esc_html__('Hello', 'geniuscourse'); // возвращает результат перевода
	$hello = esc_html_e('Hello', 'geniuscourse'); // выводит на экран результат перевода
```

Далее нужно сказать WordPress'у где хранится директория с языковыми файлами. Для этого используется хук `after_setup_theme` и в него нужно дописать вызов функции `load_theme_textdomain()` с двумя параметрами:

1. textdomain. У меня - это `'geniuscourse'`;
2. путь до языового файла, я изменил название директории _languages_ на _lang_ для экперимента и у меня теперь - это `/lang`.

Получилось так: `load_theme_textdomain('geniuscourse', get_template_directory() . '/lang');`

Удаляю вызов функции `load_theme_textdomain()` в коде болванки ниже.

Следующим шагом генерирую новый файл _geniuscourse.pot_. Делается это с помощью программы _poEdit_.

Только вот че я ни делал, не получилось у меня перевести с английского на русский. Все делал по инстукции Сокирки. То ли poEdit неверно работает, толи я что-то не то делаю, толи WordPress с Underscores более новые стали. В общем не пойму пока. Разберусь позже.

Все комментарии урока в файле _header.php_ данного коммита.

Документация WordPress по интернационализации темы:

1. Подготовка темы к интернационализации - https://developer.wordpress.org/themes/functionality/internationalization/
2. Скачать poEdit - https://poedit.net/download

### 22. Шаблоны для страниц (Custom Page Templates)

Из этого урока я узнаю, что такое page template. По русски - это шаблон для конкретной страницы.

В Админке - Настройки - Чтение: Настройки чтения, "На главной странице отображать": по умолчанию установлен "Ваши последние записи". Это значит, что за главную страницу отвечает файл _index.php_. На главно сейчас не контента, потому что удалена директория по умолчанию _template_parts_. Чтобы ее немного заполнить выведу заголовок: `the_title()` и удалю из кода вывод директории по умолчанию. Также закомментировал `get_sidebar()`, чтобы было меньше контента на странице, чтобы лишний контент не мешал просмортру того, что происходит. Далее создал в Админке еще один пост для вывода на главной странице списка постов. Обновил Главную, все работает, выводится Header, Footer, и список постов.

Если нужна конкретная страница на Главной, то в Админке в настройках чтения, "На главной странице отображать" выбираю "Статическая страница" и в списке выбираю ту страниц, которая мне нужна. При перезагрузке во фронте подгружается уже другая страница, за которую уже отвечает файл _page.php_. Но в этом файле напсан шаблон для всех страниц. Для этого в WordPress есть опция Custom Template. В корне темы добавляю файл _template-home.php_. Внутри этого файла в первую очередь пишется комментарий:

```
<?php
/**
 * Template name: Домашняя страница
 */
```

Далее иду в Админку, редактирую выбраную главной страницу, справа выбираю шаблон, в выпадающем списке будет мой шаблон (Домашняя страница), сохраняю. Теперь за эту страницу будет отвечать файл _template-home.php_, а за остальные страницы _page.php_.

Есть второй способ с которым я познакомлюсь далее по курсу. Пока я его не знаю, кроме названия: Мета-блоки.

Третий способ: жестко привязывать страницу к файлу.
В Админке добавляю страницу, например "About" и перехожу на нее во фронт. Убеждаюсь, что за нее отвечает _page.php_, добавив в коде этого файла вывод какой-нибудь строки. Далее создаю в корне темы файл _page-about.php_, в нем делаю верстку, во фронте обновляю страницу и занее уже отвечает новый файл _page-about.php_. Этот способ не подойдет для разработки темы помому что заранее неизвестно какие страницы будут на сайте. Если сайт делается для клиента или для себя, то можно так сделать, но для разработки темы - нет.

Документация WordPress по шаблонам для страниц:

1. https://developer.wordpress.org/themes/template-files-section/page-template-files/

### 23. Вывод постов в цикле

Настраиваю админку для этого урока: Админка->Настройка чтения->Главная - Ваши последние посты. Проверяю сайт, обновляя Главную. Вижу последние посты. Значит работает файл _index.php_. Удаляю весь HTML-код от `<main>` до `</main>`. Остаются только php-функции вывода Шапки, Подвала, закомментированного Сайдбара и комментарий описания файла, который тоже можно удалить. Пишу свой код:

```
<div>
	<?php if (have_posts()) : /* Посты есть */ ?>
		<?php while (have_posts()): /* Перебор постов */?>
			<?php the_post() ?>
			<?php get_template_part('partials/content') /* Подключаю шаблон контента */ ?>
			<br>
		<?php endwhile ?>
	<?php else : /* Постов нет */ ?>
		<?php get_template_part('partials/content', 'none') /* Подключаю шаблон отсутствия контента */ ?>
	<?php endif ?>
</div>
```

Проверяю фронт, вижу 2 ссылки на пост, потому что в админке 2 поста. Разбирая вышенаписанный код можно понять, что в файле _index.php_ писать верстку для вывода постов не резонно. Эта часть верстки будет писаться в файле _partials/content.php_:

```
<article <?php post_class() /* Подключение классов */ ?> id="post-<?php the_ID() /* ID поста */ ?>" data-post-id="<?php the_ID() /* ID поста */ ?>">
    <h1><?php the_title() /* Вывод заголовка поста */ ?></h1>
    <div>
        <div>
            <?php the_content() /* Вывод контента */ ?>
        </div>
        <a href="<?php the_permalink() /* Ссылка на пост */ ?>"><?= esc_html__('Read more', 'geniuscourse') /* Читать далее */ ?></a>
    </div>
</article>
```

Если постов нет, то в _index.php_ во втором условии (`esle`) прописывается шаблон выводящий сообщение об отсутствии постов:

```
<span><?= esc_html__('No posts', 'geniuscourse') ?></span>
```

Заметка:

- у функций, таких как `the_title()`, `the_content()` есть гетеры-аналоги `get_the_title()`, `get_the_content()`. Гетеры отличаются от этих функций тем, что они не выводят на экран, а возвращают значения. И чтобы с ними вывести на экран необходимо перед ними написать `echo` или в HTML-коде написать такую конструкцию: `<?= get_the_title() ?>`

Документация WordPress по циклам:

1. https://codex.wordpress.org/The_Loop

### 24. Регистрация Post Type. #1

Сейчас обучаюсь Custom Post Type.

В Админской панели есть отделные разделы "Посты" и "Статьи", но в БД они хранятся в одной таблице, отличаются только флагом _Post Type_. Можно сделать свой Пост Тайп и в Админской панели появится новый раздел.

К примеру, нужен раздел "Машины". Регистрируется новый Пост Тайп "Car" и появляется соответствующий раздел. Главное, чтобы название не повторяло ряд уже зарегистрированных Пост Тайпов.

Для регистрации Пост Тайпа нужно перейти в файл _functions.php_, создать новый хук:

```
function geniuscourse_register_post_type()
{
	register_post_type('car', $args);
}
add_action('init', 'geniuscourse_register_post_type');
```

Функция `register_post_type()` регистрирует Пост Тайп. Содержит 2 параметра:

1. название Пост Тайпа;
2. массив аргументов.

Массив аргументов описан в комментариях в коде _functions.php_. А также все описание можно найти в документации по ссылке ниже.

Но регистрировать Пост Тайп внутри темы не рекомендуется. При смене темы пользователь потеряет Пост Тайп. Поэтому нужно его выносить в отдельный плагин. Сейчас Пост Тайп регистрируется внутри темы для общего развития. Если делать сайт под ключ для клиента, фриланса, для себя, то это вполне допустимо. Но для разработки темы это запрещено, только в отдельном плагине.

Документация WordPress по регистрации Пост Тайпов:

1. Регистрация постайпа - https://developer.wordpress.org/reference/functions/register_post_type/
2. Иконки - https://developer.wordpress.org/resource/dashicons/#cover-image

### 25. Регистрация Post Type. #2

Для того, чтобы работала _thumbnails_ необходимо, чтобы была включена поддержка _post_thumbnails_. Эта поддержка включена Underscores в файле _functions.php_. Перенес вызов функции в тело своей функции `geniuscourse_theme_init()`:

```
add_theme_support('post-thumbnails');
```

Там же добавил поддержку _post-formats_:

```
add_theme_support('post-formats', [
	'video',
	'image',
	'quote',
	'gallery',
]);
```

Также добавил условие, чтобы Пост Тайп привязался к этим форматам:

```
add_post_type_support('Car', 'post-formats');
```

это удобно тем, что можно создавать различные форматы для различных Пост Тайпов. В Амдинке при добавлении или редактировании поста (элемента Пост Тайпа) появилась возможность выбирать формат.

В аргументах (`$args`) для ЧПУ-ссылок нужно вставить параметр `rewrite` со значением `['slug' => 'cars']`. Теперь для перехода на страницу будет испольоваться домен `https://genius.loc/cars`.

Для включения редактора _Gutenberg_ нужно включить параметр `['show_in_rest' => true]`. Чтобы редактор _Gutenberg_ работал необходимо в массив параметра `supports` добавить элемент `editor`.

Чтобы не слетала работа ЧПУ-ссылок при обновлении темы нужно добавить в _functions.php_ функцию `function geniuscourse_rewrite_rules()`:

```
function geniuscourse_rewrite_rules()
{
	geniuscourse_register_post_type();
	flush_rewrite_rules();
}
```

Этот код нужно вставлять при регистрации Пост Тайпа, чтобы в дальнейшем не было проблем с ошибкой 404 при работе с новыми Пост Тайпами.

### 21. Подготовка файлов темы к переводу #2

Как ни колдовал я с файлам _ru_RU.po_ и _ru_RU.mo_, так у меня ни чего и не вышло, НО...

Может быть это фишка WordPress **7.0**? Вобщем переименовал я папку _/lang_ обратно в _/languages_ и все заработало. Более того - закомментировал в _functions.php_->`geniuscourse_theme_init()` вызов функции `load_theme_textdomain('geniuscourse', get_template_directory() . '/languages')` и все равно все работает. Надо будет проштудировать _7.0_ версию WordPress на многоязычность. Благо есть ссылка на документацию.

А в документации говорится, что вызывать языковую функцию в идеале нужно из файла _functions.php_. Выше у меня написано, со слов автора курса, что это не самый хороший вариант. В общем оставил я вызов языковой функции в файле функций и переименовал ранее переименованную папку языков обратно в _/languages_. Все работает всем спасибо, и мне тоже.

### 26. Шаблон архивной и сингл страниц для Post Type

В этом уроке разговор пойдет об архивных и сингл страницах Пост Тайпа.

Открыв страницу с автомобилями, открывается файл _archive.php_, если перейти на страницу архивов (по дате), так же отработает этот же файл. Значит необходимо разделить работу архивных и открытых страниц.

В Админке убрал все лишние страницы из раздела _Cars_. И добавил более логичные страницы BMW и Mersedes. Но на странице не видно контента потому что страница пытается открыть удаленную директорию _template-parts_. Вставлю в файле _archive.php_ подговленный HTML-код из файла _index.php_, но оставлю только код, который динамически генерирует заголовки:

```
<header class="page-header">
	<?php
	the_archive_title('<h1 class="page-title">', '</h1>');
	the_archive_description('<div class="archive-description">', '</div>');
	?>
</header><!-- .page-header -->
```

также отключу сайдбар, чтобы не мешал, я с ним пока не работаю.

Захожу на фронт, обновляю - все работает. Но я не хочу использовать этот шаблон. Этот шаблон пусть будет для технических страниц WordPress, а мне нужен свой, со своей персональной версткой. Для этого я создаю файл archive с названием Пост Тайпа: _archive-car.php_. В WordPress для Пост Тайпа _car_ приоритеом теперь будет новый созданный файл. В новй файл скопирую верску из _archive.php_ и добавлю своей верски, что бы было видно разницу между страницами.

Теперь открыв страницу автомобилей я увижу, что это новая страница, которая подгружается из файла _archive-car.php_, а если открою архивную, то у нее будет старая верстка из файла _archive.php_.

Зайдя по ссылке "Подробнее" будет загружаться страница с автомобилями. Это открытая страница, которая подгружается с файла _single.php_. Это файл будет применяться для открытого блога, но для Пост Тайпа нужно, как правило, создаются свои персональные посты, в случае если для Пост Тайпа _car_ необходим свой дизайн (верстка) страницы. Если же все посты, включая Пост Тайп _car_ будут иметь один и тот же вид, то достаточно файла _single.php_.

Создаю файл _single-car.php_. Добавлю в него вывод Шапки, Подвала. Даже если в разрабатываемом Пост Тайпе не нужны будут комментарии, нужно оставить польователю возможность включать-отключать комментарии. Переношу из _single.php_ цикл вместе с комментариями. В вызове функции `get_template_part('partials/content', get_post_type())` путь с _template-parts/content_ на _partials/content_. В директории _partials_ создаю новый _content-car.php_ вставляю в него код из _content.php_ немного видоизменяя: добавляю свой класс в тег `<article>`, персональную верстку, для отличия во фронте. Теперь система будет искать файл _partials/content-car.php_.

Подведу итоги урока:

Если мне нужно для Пост Тайпа менять верстку в _single.php_ за пределами цикла, то целесобразно создать файл _single-car.php_. Если же достаточно изменить карточку продукта (авто в данном случае), до файл _single-car.php_ нужно удалить и пользоваться _partials/content-car.php_, а в _single.php_ указать правильный путь до контента.

Проверяю не удаляя пока файл _single-car.php_, а переименовывая его, чтобы система его не нашла, происываю в _single.php_ правильный путь к контенту. Обновляю страницу - все работает.

### 27. Регистрация Taxonomy

В этом уроке я буду создавать Taxonomy. **Что такое Таксономи?** В Админке, в разделе "Записи" ("Posts") есть две подкатегории: "Рубрики" ("Categories") и "Метки" ("Tags"). В этих подкатегория можно добавлять дополнительные параметры, т.е. дополнительные рубрики и метки и разделять контент по этим параметрам (по категориям и тегам). Вот эти разделы, "Рубрики" и "Метки" и есть Таксономи. Если зайти в "Рубрики" и посмотреть в адресную строку, то в адресе (_https://genius.loc/wp-admin/edit-tags.php?taxonomy=category_) видно, что эта Таксономи называется _category_, в "Метках" это будет _post_tag_.

Если в WordPress есть такая возможность, то можно и в свой персонализированный Пост Тайп добавить свои Таксономи. Зачем нужны Таксономи, они будут разделять контент по различным категориям. Для моей категории "Машины" ("Автомобили") можно разделить, например по бренду, году выпуска, узлам автомобиля (кузов, двигатель...).

Регистрируется Таксономи хуком `init`, т.е. тамже где регистрировался Пост Тайп - `function geniuscourse_register_post_type()`. Автор курса рекомендует регистриовать Таксономи перед регистрацией Пост Тайпа.

Прописываю следующий код:

```
$args = []; // Здесь задаются параметры аргументов, см. файл functions.php

// Далее регистрация Таксономи
// Параметры:
// 1. название Таксономи;
// 2. массив Пост Тайпов для которых регистриуется Таксономи;
// 3. список аргументов
register_taxonomy('brend', ['car'], $args);

unset($args); // очищаю список аргументов для последующего заполнения его в нуждах Пост Тайпа.
/// Ниже по коду регистрирую Пост Тайп.
```

Список аргументов см. в файле _functions.php_. Там все аргументы с комментариями.

Есть здесь еще одно понятие Term. Термы - это дети Таксономи, т.е. в подразделе "Автомобили" есть дети: "BMW", "Mersedes", "VolksWagen", и т.д.

При регистрации Таксономи нужно обращать внимание на зарезервированные Таксономи (ссылка ниже), которые нельзя изпользовать.

Документация WordPress по регистрации Taxonomy:

1. Документация https://developer.wordpress.org/reference/functions/register_taxonomy/
2. Зарезервированные Таксономи https://developer.wordpress.org/reference/functions/register_taxonomy/#reserved-terms

### 28. Шаблон архивной страницы для Taxonomy

На этом уроке я научусь создавть архивные страници для своих Таксономи. В уроках о Пост Тайпе я создавал архивные странички для Пост Тайпа. Тоже самое можно сделать и для Таксономи. Из Админки можно посмотреть архивную страницу перейдя по ссылке в фронт часть сайта, например: _https://genius.loc/brands/bmw/_.

Еще раз о приоритетах файлов в WordPress:

1. Если нет архивных, открытых и других страниц открывается _index.php_.
2. Дальше, для архивных страниц, приоритет у файла _archive.php_.
3. Если говорить о Таксономи, то это _taxonomy.php_. Этот шаблон будет применяться ко всем Таксономи. Если его нет будет применяться _archive.php_.

Создавать файл _taxonomy.php_ смысла нет, т.к. он будет применяться ко всем Таксономи. Лучше создать файл к конкретному Таксономи, например _taxonomy-brand.php_.

**Столкнулся с проблемой**:

- из Админки перехожу на просмотр Таксономи по ссылке _https://genius.loc/brands/bmw/_;
- открывается архивная страница Пост Тайпа с адресом: _https://genius.loc/cars/bmw/_.

Нашел **решение**: обновить правила перезаписи, т.е. зайти в Админке в "Настройки"->"Постоянные ссылки" и сохранить не внося изменений.

Создал два файла Таксономи: _taxonomy-brand.php_ и _taxonomy-manufacture.php_ и в них прописал соответственно `test brand` и `test manufacture`. При открытии этих таксономи я вижу соответствующий текст (не обошлось без пляски с бубном вышеописанной проблемы). Если удалить файл, например _taxonomy-manufacture.php_, то вместо нее будет подгружаться _archive.php_.

Если задать шаблон на _brend_, то у меня всегда будет этот шаблон, будь я на BMW или на Mersedes.

Если же необходимо сделать отдельный шаблон для BMW (или для какой-нибудь другой марки), то к файлу необходимо будет дописать через дефис название этого Терма: _taxonomy-brand-bmw.php_. Но этого делать не стоит по причине неизвестности какие марки автомобилей у пользователя будут внесены.

А если контент по Таксономи будет отличатся только карточками, то и разделять на разные Таксономи тоже не стоит достаточно будет сделать шаблон для всех Таксономи: _taxonomy.php_.

Удаляю файл _taxonomy-manufacture.php_ и создаю верстку в _taxonomy-brand.php_ (см. верстку в файле этого коммита).

- Перехожу на страницу BMW и вижу написанный шаблон.
- Перехожу на страницу Mersedes и вижу, что постов для нее нет. Проверяю в Админке: да, постов нет.
- Перейдя по ссылке _https://genius.loc/manufactures/2024/_ будет подгружаться архивная страница, потому что для _manufacture_ нет файла _taxonomy-manufacture.php_. Но его также можно создать и задать в нем персональный шаблон.

**Еще несколько функций, которые могут пригодиться в работе с Таксонами при разработке своей Темы**:

- `is_tax()`: bool. Возвращает `true` если загружаемая страница с Таксономи, в противно случае `false`, подробнее в файле _header.php_;
- `taxonomy_exist()` для проверки, существует ли такая Таксономи; вполне возможно, особенно при работе с множеством плагинов, что Таксономи могут дублироваться, подробнее в файле _functions.php_ в коде при регистрации Таксономи _brand_.
